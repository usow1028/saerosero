{
  "name": "saerosero",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "seed:catalog": "node scripts/seed-catalog.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "vite": "^6.2.0",
    "vitest": "^3.0.5"
  }
}
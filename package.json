{
  "name": "saerosero",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "seed:catalog": "node scripts/seed-catalog.mjs",
    "seed:posters": "node scripts/generate-posters.mjs",
    "seed": "npm run seed:catalog && npm run seed:posters",
    "test": "vitest run"
  },
  "devDependencies": {
    "vite": "^6.2.0",
    "vitest": "^3.0.5"
  }
}
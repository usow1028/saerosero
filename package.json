{
  "name": "saerosero",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "seed:catalog": "node scripts/seed-catalog.mjs",
    "seed:posters": "node scripts/generate-posters-rich.mjs",
    "seed:posters:jpg": "node scripts/render-posters-jpg.mjs",
    "seed:posters:composite": "node scripts/composite-posters.mjs",
    "seed:sync": "node scripts/sync-ai-posters.mjs",
    "seed": "npm run seed:catalog && npm run seed:posters && npm run seed:posters:jpg && npm run seed:posters:composite && npm run seed:sync",
    "test": "vitest run"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "vite": "^6.2.0",
    "vitest": "^3.0.5"
  }
}
